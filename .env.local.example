# Optional local-machine overrides for `npm run dev:backend`.
# Copy to `.env.local` when the backend runs on the host while Postgres/MCP run in Docker.

DATABASE_URL=postgresql://travel_assistant:change-me@localhost:5432/travel_assistant
XHS_MCP_URL=http://localhost:18060/mcp
