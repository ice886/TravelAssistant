# 2026_07_12

## 工作内容

- 按确认计划移除 Research 结果缓存机制：删除 `research_cache` 服务、类型、测试、缓存分支和来源物化逻辑；每次 Research run 重新执行 Agent 与可用 Provider。
- 移除 `RESEARCH_CACHE_TTL_SECONDS` 配置及前端缓存命中状态，保留同一 trip 单活保护和 stale run 清理；数据库启动时清理旧版本遗留的 `research_cache` 表。
- 同步更新 v2 architecture、PROJECT_STRUCTURE、Research 执行技术文档、Planner 技术文档、前端功能文档和 README，并新增连续 Research run 回归测试。

- 确认并落盘 ReAct Research 与缓存计划。
- 将固定 provider 顺序重构为受约束 Research Agent，支持五个白名单工具、最多 8 轮、严格 decision 校验、重复调用去重和工具失败降级。
- 将 Research POST 调整为创建 run 后立即返回，API 进程内继续执行，前端轮询展示实际轮次和工具安全摘要。
- 新增 PostgreSQL `research_cache`、规范化 SHA-256 key、7 天默认 TTL；缓存命中为新 run 物化 fresh source IDs。
- 缓存 key 纳入预算、旅客、模型与 provider profile；缓存 envelope 保留降级覆盖，运行时 provider 失败结果不缓存。
- 增加单 trip active-run 部分唯一索引、重复启动复用和遗留 running run 超时处理。
- 小红书未配置、未登录、状态未知、超时或调用失败时自动降级；新增 MCP 请求超时，不持久化 xsec token。
- 扩展高德实时天气/预报和步行、驾车、公交路线能力。
- 按高德接口契约先将天气城市名解析为 adcode，并拒绝空天气/空路线证据。
- Planner 改为读取最新已完成且有来源的 research run，避免空 running run 遮蔽证据。
- 前端提升 current run 状态，展示缓存、降级、Agent 轮次和工具记录；来源面板按 run 生命周期轮询并区分天气/路线来源。
- 增加 ReAct、缓存、工具、provider、后台终态、Research API 和前端展示测试。
- 同步 README、architecture、guide、technical、环境变量与 Compose 配置。
- 新增 `.dockerignore`，排除 `.env`、依赖、构建产物与 XHS Cookie 数据。
- 使用 designer-skills 的 design brief、情绪化界面、响应式和无障碍方法完成前端基础体验重设计。
- 新增三阶段旅行导航，统一色彩、字体、间距、卡片、按钮和异步状态反馈，并优化 Research 等待、降级、失败与恢复文案。
- 完成桌面、平板和 390px 移动端视觉验收；移动端无横向溢出，应用控制台无警告或错误。
- 简化 Research 来源结果项，仅展示简体标题、简体摘要与“查看原始来源”链接；新增展示层繁转简和空摘要占位。
- 进一步收紧 Tavily 网页来源展示，仅保留标题和原始来源链接；小红书与高德继续展示摘要。

## 影响范围

- `backend/src/modules/research/`
- `backend/src/modules/providers/`
- `backend/src/modules/mcp/`
- `backend/src/modules/config/`
- `backend/src/modules/database/`
- `backend/src/modules/planner/`
- `frontend/src/app/`
- `frontend/src/api/`
- `frontend/src/features/agent-run/`
- `frontend/src/features/sources/`
- `.env.example`、`docker-compose.yml`、`README.md`
- `Docs/architecture/`、`Docs/guide/`、`Docs/technical/`、`Docs/plan/`
- `Docs/feature/1-FRONTEND_UX_WORKSPACE.MD`

## 验证

- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run build`
- `docker compose config --quiet`
- 使用现有 PostgreSQL 持久卷完成旧 schema 升级 smoke：`progress`、`research_cache`、单活/查询索引均存在；旧 run API 进度归一化通过。
- 在事务回滚内验证单活 `ON CONFLICT` 与缓存 CTE 原子物化 SQL。
- Compose 镜像重建曾在远程 Dockerfile frontend 解析阶段无输出卡住并已主动终止；改用本地 production build 在 3001 端口完成 API 启动与健康检查，未替换现有容器。
- 前端重设计后再次通过 `npm run lint -w frontend`、`npm run typecheck -w frontend`、`npm run test -w frontend`（9 tests）和 `npm run build -w frontend`。
- Research 来源简化后通过前端 lint、typecheck、10 项测试与 production build。
- Tavily 摘要隐藏修复通过前端 lint、typecheck、11 项测试与 production build。
- 采用 `design-critique`、`information-architecture`、`navigation-patterns`、`visual-hierarchy`、`responsive-design` 和 `accessibility-audit` 优化单页工作台：按阶段只展示当前工作区，增加阶段前置条件、上一步/下一步操作和研究完成自动推进。
- 阶段工作台优化通过前端 lint、typecheck、11 项测试与 production build。
- 第三阶段按参考图改为紧凑只读旅行计划，移除前端行程编辑组件和保存控件，保留计划生成与读取。
- 旅行计划紧凑化通过前端 lint、typecheck、11 项测试与 production build。
- 将 Hero 标题改为“去有风的地方”，缩短首屏高度；更新旅行计划说明文案并移除前端“可编辑/修改”表述。
- 为旅行计划活动卡片新增 24px 线性 SVG 图标，根据交通、艺术、自然、地标和餐饮内容匹配图标与颜色。
- 增加驾车/自驾关键词识别和汽车线性 SVG 图标，补充蓝色交通状态样式。
- 修正活动图标匹配优先级：活动内容优先于交通方式，避免“蒙餐馆 · 自驾”误显示汽车；新增摄影/拍照相机图标，并增加 3 个匹配回归测试。
- 进一步增加自驾路线意图规则：出发、前往、返程、还车、公路等标题/地点与自驾组合时优先显示汽车，覆盖摄影描述导致的误判；回归测试累计 15 项。

## Agent Core Framework 重构

- 新增 `agent-core` 通用 Agent 框架：`BaseAgent`、`BaseTool`、`ToolRegistry`、`AgentRunner` 和 `AgentOrchestrator`。
- Research 工具拆分为五个独立工具类，并通过注册器接入；ResearchService 改由通用 Runner 执行 ResearchAgent。
- 删除旧的 `ResearchAgentService` 与 `ResearchToolsService`，保留 Research API、数据库和 Provider 契约。
- 验证：backend typecheck 与 36 项测试通过；全 workspace lint/build 待完成。

## Agent-First 后端分层重构

- 覆盖原 13 号计划，按 `agent-core → agents → workflows → persistence → api → infrastructure → mcp` 重组后端目录。
- 新增 `PlannerAgent`、`ResearchWorkflow`、`PlanWorkflow`、四类 Repository、统一 API Controller 和 Infrastructure 聚合模块。
- `AgentRunner` 增加 `generate` 单次结构化输出模式；保留 Research 的异步、降级、进度和 API 行为。
- NestJS 启动 smoke 通过，路由仍覆盖 `/api/health`、`/api/trips`、Research、Planner 和 XHS API。
