# 2026_07_10

## 工作内容

- 修复 backend/frontend 两个 TypeScript workspace 的 ESLint parserOptions 根目录不明确问题。
- `backend/eslint.config.js` 显式设置 `parserOptions.project = "./tsconfig.json"` 与 `tsconfigRootDir = __dirname`。
- `frontend/eslint.config.js` 显式设置 `parserOptions.project = "./tsconfig.json"`，并基于 `import.meta.url` 计算 frontend 的 `tsconfigRootDir`。
- 新增 `Docs/issue/fix/1-ESLINT_TSCONFIG_ROOTDIR.MD`，记录现象、修复动作和验证结果。
- 运行并通过 `npm run lint`、`npm run typecheck`、`npm run test`。
- 完成 `waiting_login` 登录恢复流程：Agent 面板支持获取/刷新小红书登录二维码，扫码后重新检查并恢复 research preflight。
- 新增前端小红书二维码 API 客户端测试，并更新 README、项目结构和 MCP 安全技术文档。

## 影响范围

- `backend/eslint.config.js`
- `frontend/eslint.config.js`
- `Docs/issue/fix/1-ESLINT_TSCONFIG_ROOTDIR.MD`
- `Docs/plan/7-XHS_LOGIN_RECOVERY.MD`
- `frontend/src/features/agent-run/AgentRunPanel.tsx`
- `frontend/src/api/client.test.ts`
- `frontend/src/app/styles.css`
- `README.md`
- `Docs/guide/PROJECT_STRUCTURE.MD`
- `Docs/technical/2-XHS_MCP_STATUS&SAFETY.MD`
- 修复小红书登录成功后右上角状态不刷新的问题：Agent 面板将最新 MCP 登录状态回传给 App 顶部状态栏。
- 新增 `Docs/issue/fix/2-XHS_STATUS_REFRESH_AFTER_LOGIN.MD` 记录现象、根因、修复动作和验证结果。
- 新增 `Docs/plan/8-RESEARCH_SOURCE_COLLECTION.MD`，记录真实来源研究闭环计划。
- Research run 从预检推进到小红书只读、高德 POI、Tavily 网页来源采集，并将受限摘要持久化到 `research_sources`。
- 新增 `GET /api/trips/:id/research-sources`，前端来源面板按当前 trip 自动刷新真实来源。
- 更新 research 技术文档、项目结构说明和 README。
- 再次运行并通过 `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`。
- 新增 `Docs/issue/fix/3-XHS_SEARCH_STRATEGY.MD`，记录小红书无 provider 来源的根因与修复。
- 修复小红书搜索策略：按目的地、目的地+旅行、目的地+兴趣逐级降级，并增加当前常见英文目的地别名归一化。
- 修复小红书 `noteCard` 结果解析，保存标题、作者、摘要、笔记 ID、xsec token 和来源 URL。
- 新增 research fallback 与 noteCard 解析测试，全部质量检查通过。
- 对照官方 `xpzouying/xiaohongshu-mcp` 实现，修复 Compose 的 `COOKIES_PATH`、浏览器缓存目录和容器运行参数，确保 Cookie 持久化到 `docker/xhs/data/`。
- 迁移本地 MCP Cookie 到持久化目录并重建官方 MCP 容器；真实 backend research 已成功落库 5 条 `xiaohongshu` 来源、1 条高德来源和 5 条 Tavily 来源。
- MCP 客户端新增 `isError` 传播与测试，避免官方搜索错误被静默识别为空结果。
