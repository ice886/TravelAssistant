# 2026_07_09

## 工作内容

- 诊断 `docker compose up --build -d` 在 `api` 镜像 `npm ci --workspace backend` 阶段因 `ECONNRESET` 中断的问题。
- 新增根目录 `.npmrc`，统一 npm 安装的重试次数、重试间隔、拉取超时和日志噪声配置。
- 更新 `backend/Dockerfile` 与 `frontend/Dockerfile`，构建时复制 `.npmrc` 并启用 BuildKit npm 缓存，降低并行构建时重复拉包和网络抖动影响。
- 更新 README 的 Docker Compose 说明，记录构建安装依赖时的网络可靠性策略。
- 新增 `Docs/plan/2-TRIPS_API_AND_STATUS_INTEGRATION.MD`，记录旅行计划 API 和前端状态接入计划。
- 后端新增 `database` 与 `trips` 模块，使用 `pg` 连接 Postgres 并提供 `POST /api/trips`、`GET /api/trips`、`GET /api/trips/:id`。
- 后端按 `WEB_ORIGIN` 启用 CORS，支持本地 Vite 前端直连 API。
- 前端状态栏接入 `GET /api/health`，旅行表单接入 `POST /api/trips`。
- 新增 `Docs/technical/1-TRIPS_API&DATABASE.MD`，沉淀 Trips API、数据表和前端接入契约。
- 运行并通过 `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`。
- 新增 `Docs/plan/3-FRONTEND_CHINESE_LOCALIZATION.MD`，记录前端中文化计划。
- 将前端用户可见文案改为中文，包括工作台标题、状态栏、旅行计划表单、Agent 进度、来源证据和行程编辑占位内容。
- 将旅行表单默认选项改为中文，并同步更新前端测试。
- 新增 `Docs/plan/4-XHS_MCP_STATUS&SAFETY.MD`，记录小红书 MCP 状态和只读安全层接入计划。
- 后端新增 `safety` 模块，维护小红书 MCP 只读白名单和写操作黑名单。
- 后端新增 `mcp` 模块，通过 JSON-RPC over Streamable HTTP 调用小红书 MCP，并提供 `GET /api/xhs/status` 与 `GET /api/xhs/login-qrcode`。
- 修复 MCP 工具返回多段 content 时只保留首段文本的问题，保留二维码图片 content，并新增 `GET /api/xhs/login-qrcode/image` 供直接扫码。
- 前端状态栏接入 `GET /api/xhs/status`，展示小红书 MCP 连接和登录状态。
- 新增 `Docs/technical/2-XHS_MCP_STATUS&SAFETY.MD`，沉淀 MCP 状态 API、安全边界和前端接入规则。
- 调整后端构建脚本和 `tsconfig.build.json`，避免 `src/**/*.test.ts` 被编译进 `dist` 后干扰 Vitest。
- 后端启动入口新增 `dotenv` 加载，按 `.env` 后 `.env.local` 的顺序读取配置；新增 `.env.local.example` 供本机后端覆盖 Docker 服务名为 `localhost`。
- 再次运行并通过 `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`。

## 影响范围

- `.npmrc`
- `backend/Dockerfile`
- `frontend/Dockerfile`
- `README.md`
- `backend/src/modules/database/`
- `backend/src/modules/trips/`
- `frontend/src/api/client.ts`
- `frontend/src/app/App.tsx`
- `frontend/src/features/trip-form/TripForm.tsx`
- `frontend/src/features/trip-form/tripDefaults.ts`
- `frontend/src/features/trip-form/tripDefaults.test.ts`
- `frontend/src/features/agent-run/AgentRunPanel.tsx`
- `frontend/src/features/sources/SourcesPanel.tsx`
- `frontend/src/features/itinerary-editor/ItineraryEditor.tsx`
- `backend/src/modules/safety/`
- `backend/src/modules/mcp/`
- `backend/package.json`
- `backend/src/main.ts`
- `backend/tsconfig.build.json`
- `.env.local.example`
- `package-lock.json`
- `Docs/plan/2-TRIPS_API_AND_STATUS_INTEGRATION.MD`
- `Docs/plan/3-FRONTEND_CHINESE_LOCALIZATION.MD`
- `Docs/plan/4-XHS_MCP_STATUS&SAFETY.MD`
- `Docs/technical/1-TRIPS_API&DATABASE.MD`
- `Docs/technical/2-XHS_MCP_STATUS&SAFETY.MD`
- `Docs/guide/PROJECT_STRUCTURE.MD`
- `Docs/architecture/BACKEND&FRONTEND_ARCHITECTURE.MD`
